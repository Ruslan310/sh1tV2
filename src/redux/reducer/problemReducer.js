import {

} from "../action";

const initialState = {
    tableReport: null,
    problemComment: null,
    problemNumber: null,
}

export const problemReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}